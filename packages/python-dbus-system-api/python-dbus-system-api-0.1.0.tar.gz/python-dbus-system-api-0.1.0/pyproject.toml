[tool.poetry]
name = "python-dbus-system-api"
version = "0.1.0"
description = ""
authors = ["Manuel Brea <m.brea.carreras@gmail.com>"]
readme = "README.md"
packages = [{ include = "python_dbus_system_api" }]

[tool.poetry.scripts]
pysysapi = "python_dbus_system_api.main:start_server"

[tool.poetry.dependencies]
python = "^3.10"
dbus-next = "^0.2.3"
pulsectl-asyncio = "^0.2.3"

[tool.pyright]
venvPath = "."
venv = ".venv"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
