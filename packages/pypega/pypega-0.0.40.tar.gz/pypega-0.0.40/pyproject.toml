[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "pypega"
version = "0.0.40"
authors = [
  "Rob Smart <rob.smart@pega.com>",
]
maintainers = [
  "Rob Smart <rob.smart@pega.com>",
  "James Sleightholm <james.sleightholm@pega.com>"
]
description = "Python package for orchestrating Pega environments"
readme = "README.md"
license = "LICENSE"
keywords = ["pega", "bpm", "pypega"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]
exclude = [
    { path = "tests", format = ["sdist", "wheel"] },
    { path = "__package", format = ["sdist", "wheel"] },
    { path = "__samples", format = ["sdist", "wheel"] },
    { path = ".gitignore", format = ["sdist", "wheel"] }
]

[tool.poetry.dependencies]
requests-oauthlib = "^1.3.0"

[project.urls]
"homepage" = "https://pypi.org/project/pypega/"
"documentation" = "https://pypi.org/project/pypega/"
"repository" = "https://pypi.org/project/pypega/"
