# Template to create a usernamePassword Credential using JCasC

credentials:
  system:
    domainCredentials:
      - credentials:
        - usernamePassword:
            description: Created with JCasC and k9.
            id: {{secretName}}
            password: {{password}}
            username: {{username}}
            scope: GLOBAL