[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
version = "1.0.1"
name = "hc_log_tools"
authors = [
  { name="xukai.sh", email="shixukai@163.com" },
]
readme = "README.md"
requires-python = ">=3.7"
dynamic = ["dependencies"]
[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}