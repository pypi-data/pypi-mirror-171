credentials:
  system:
    domainCredentials:
      - credentials:
        - string:
            description: {{secretName}}
            id: {{secretName}}
            secret: {{secret}}
            scope: GLOBAL