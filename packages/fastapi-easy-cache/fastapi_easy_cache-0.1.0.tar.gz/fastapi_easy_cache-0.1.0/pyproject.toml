[tool.poetry]
name = "fastapi-easy-cache"
version = "0.1.0"
description = "A simple tool for caching fastapi response"
authors = ["dregg"]
readme = "README.md"
packages = [{include = "fastapi_easy_cache"}]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
