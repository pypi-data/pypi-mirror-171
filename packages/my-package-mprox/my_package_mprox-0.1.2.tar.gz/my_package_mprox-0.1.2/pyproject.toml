[tool.poetry]
name = "my-package-mprox"
version = "0.1.2"
description = ""
authors = ["MProx <fake@nomail.com>"]
readme = "README.md"
packages = [{include = "my_package_mprox"}]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.1"
boto3 = {version = "1.24.89", optional = true}

[tool.poetry.extras]
boto3 = ["boto3"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

