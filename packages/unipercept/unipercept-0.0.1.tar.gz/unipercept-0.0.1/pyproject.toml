[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unipercept"
version = "0.0.1"
authors = [
    { name = "Kurt H.W. Stolle", email = "k.h.w.stolle@gmail.com" }
]
description= "A unified perception system using deep learning"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[tool]
