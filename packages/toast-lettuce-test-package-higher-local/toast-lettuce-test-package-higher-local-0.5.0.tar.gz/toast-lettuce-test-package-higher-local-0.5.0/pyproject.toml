[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "toast-lettuce-test-package-higher-local"
version = "0.5.0"
maintainers = [
  {name = "Tom V"}
]
