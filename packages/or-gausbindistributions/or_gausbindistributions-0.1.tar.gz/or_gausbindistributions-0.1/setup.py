from setuptools import setup

setup(name='or_gausbindistributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['or_gausbindistributions'],
      zip_safe=False)
