[tool.poetry]
name = "functions-by-nomadstalker"
version = "0.2.0"
description = "This is our test project"
authors = ["Omurbek Orozaliev <boexpresskg@gmail.com>"]
readme = "README.md"
packages = [{include = "functions_by_nomadstalker"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
