[tool.poetry]
name = "dbs"
version = "0.1.2"
description = ""
authors = ["yeger00 <yeger00@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
databases = {extras = ["aiosqlite"], version = "^0.6.1"}
typer = "^0.6.1"

[tool.poetry.group.tests]
optional = true

[tool.poetry.group.tests.dependencies]
pytest = "^7.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
query = "dbs.query:app"
