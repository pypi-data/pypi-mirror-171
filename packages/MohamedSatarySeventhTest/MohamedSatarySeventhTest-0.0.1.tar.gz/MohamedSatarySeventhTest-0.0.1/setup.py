from setuptools import setup, find_packages

setup(
      name='MohamedSatarySeventhTest',
      version='0.0.1',
      description='''This library converts Names (or words) from arabic to its 
      Arabizi form, and vice versa (from Arabizi to arabic)''',
      #py_modules=['helloWorld', 'bye', 'goodBye'],
      #package_dir={'': 'src'},
      packages=find_packages()
      )