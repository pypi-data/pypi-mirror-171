[tool.poetry]
name = "tcd-pipeline"
version = "0.2.0"
description = ""
authors = ["chitchai <chitchaic@gmail.com>"]
readme = "README.md"
packages = [{include = "tcd_pipeline"}]

[tool.poetry.dependencies]
python = "^3.10"
PyYAML = "^6.0"
click = "^8.1.3"
jsonpath-ng = "^1.5.3"

[tool.poetry.scripts]
tcd-pipeline = "tcd_pipeline.main:cli"

[tool.poetry.group.dev.dependencies]
pytest = "^7.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
