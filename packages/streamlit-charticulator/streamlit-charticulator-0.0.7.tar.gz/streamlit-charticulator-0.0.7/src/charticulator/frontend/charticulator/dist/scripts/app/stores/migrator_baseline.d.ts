import { AppStoreState } from "./app_store";
/** Upgrade old versions of chart spec and state to newer version */
export declare function upgradeGuidesToBaseline(appStoreState: AppStoreState): AppStoreState;
