[tool.poetry]
name = "test-pkg-poetry-binlecode"
version = "0.1.1"
description = ""
license = "MIT"

authors = ["Bin Le <bin.le.code@gmail.com>"]
repository = "https://github.com/binlecode/example-python-poetry"
readme = "README.md"
packages = [{include = "test_pkg_poetry_binlecode"}]

[tool.poetry.dependencies]
python = "^3.9"


[tool.poetry.group.dev.dependencies]
pytest = "^7.1.3"
black = "^22.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
